name: Runner Probe

on:
  pull_request:
    branches:
      - main

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub-hosted Linux runner works"

  windows:
    runs-on: windows-2022
    steps:
      - run: Write-Host "GitHub-hosted Windows runner works"
        shell: pwsh
